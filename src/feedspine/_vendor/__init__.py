"""Vendored spine-core stubs for standalone operation.

These are minimal protocol/type definitions extracted from spine-core
so that feedspine can be installed and used without spine-core as a
hard dependency.  When spine-core *is* installed the vendored stubs
are not used — the real implementations are preferred.
"""
